---
expect: bluf,blast-radius,out-of-scope,fact-vs-hypothesis,next-steps,quantified,table
---

# 課題: 設計書

次の材料をもとに、チームレビューにかける設計書を日本語で書け。
材料は整理されていない。並べ方は任せる。
ファイルには書かず、設計書の本文をそのまま出力せよ。

- 通知の配信基盤を作り直したい。今は `apps/api/src/notify.ts` の 1 ファイルに、メール、Slack、Web Push の送信処理が全部入っている。1,400 行ある
- 送信先が増えるたびにこのファイルを触るので、コンフリクトが月に 3 回くらい起きている
- ジョブキューを挟む案がある。Cloud Tasks と Redis ベースの BullMQ を比べた
- Cloud Tasks はインフラの管理が不要。ただしローカル開発でエミュレータが要る
- BullMQ は既に `apps/worker` で使っていて、開発環境もある。運用の手間は Redis の分だけ増える
- 自分は BullMQ に寄せたいと思っている。ただしチームで Redis を触れる人が 2 人しかいない
- 現在の送信数はメールが日次 12,000 通、Slack が 3,000 件、Web Push が 45,000 件
- Web Push だけレイテンシ要件が厳しいらしい。誰が決めた要件なのか出典を見つけられていない
- 送信失敗時のリトライは今はない。落ちたら落ちっぱなし
- テンプレート管理も同じファイルに入っているが、今回は触らないつもり
- 移行中は新旧の経路が並走する。二重送信が起きないかが一番怖い
- `apps/api/src/notify.ts` を呼んでいるのは 23 箇所
